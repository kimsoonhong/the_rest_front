import styled from "@emotion/styled";

export default function Header() {
  return <HeaderContainer>f</HeaderContainer>;
}
const HeaderContainer = styled.div`
  background-color: red;
  height: 4rem;
  min-height: 4rem;
  position: fixed;
  width: 1280px;
`;
